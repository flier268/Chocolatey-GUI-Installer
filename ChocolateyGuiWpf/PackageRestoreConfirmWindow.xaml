<Window x:Class="ChocolateyGuiWpf.PackageRestoreConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="還原確認" Height="400" Width="700" WindowStartupLocation="CenterOwner" ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="將安裝以下套件：" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
        <DataGrid x:Name="PackageDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding}" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="套件名稱" Binding="{Binding}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="ConfirmButton" Content="確定" Width="100" Margin="5" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="取消" Width="100" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>